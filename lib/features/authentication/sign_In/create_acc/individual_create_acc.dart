import 'package:flutter/material.dart';

class IndividualPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Individual Page'),
      ),
      body: Center(
        child: Text('Welcome to the Individual Page!'),
      ),
    );
  }
}
